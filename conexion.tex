\begin{tad}{\tadNombre{Conexion}}
 
 %La igualdad observacional no sé si está bien...
 \tadIgualdadObservacional{c}{c'}{conexion}{compusQueConecta($c$) \igobs compusQueConecta($c'$) $\wedge$ (\paratodo {compu}{c1}) (conectaA(c, c1) \yluego conectaA(c', c1)) \impluego \\
								(interfaz(c, c1) \igobs interfaz(c', c1)}
 %No se qué se tiene que poner en generos, experta y usa, revisar.
 \tadGeneros{conexion}
 \tadExporta {conexion, observadores, generadores, otrasoperaciones}
 \tadUsa {Computadora, Bool}
 
 \tadGeneradores
 
 \tadOperacion{crearConexion}{tupla(compu/c, interfaz/i), tupla(compu/c', interfaz/i')}{conexion}{$i\in$ interfaces($c$) $\wedge$ $i'\in$ interfaces($c'$)}
 
 \tadObservadores
 \tadOperacion{compusQueConecta}{conexion}{conj(compu)}{}
 \tadOperacion{interfaz}{conexion/c, compu/c1}{interfaz}{conectaA(c, c1)}
 
 \tadOtrasOperaciones
 
 \tadOperacion{conectaA}{conexion, compu}{bool}{}
 \tadOperacion{laOtraCompu}{conexion/c, compu/c1}{compu}{conectaA($c$, $c1$)}
 \tadOperacion{estanConectadas}{conexion, compu, compu}{bool}{}
 
 \tadAxiomas [\paratodo{compu}{c1,c2}, \paratodo{conexion}{conexion}, \paratodo{interfaz}{i1,i2}]
 
 \tadAxioma{compusQueConecta(crearConexion(($c1$, $i1$), ($c2$, $i2$)))}{Ag($c1$, Ag($c2$, Vacio))}

 \tadAxioma{interfaz(crearConexion(($c1$, $i1$), ($c2$, $i2$)), $c$)}{\IF $c$ = $c1$ THEN $i1$ ELSE $i2$ FI} 
 
 \tadAxioma{conectaA($conexion$, $c1$)}{$c1$ $\in$ compusQueConecta($conexion$)}
 % 08/04/2015 02:42 am me di cuenta que faltaron los "{}" al c1 en la resta de conjuntos en el axioma de laOtraCompu (eze)
 % Modifiquenlo, yo no sé si va a funcionar si le pongo los corchetes asi a lo bestia, quiza no compila, no sé.
 \tadAxioma{laOtraCompu($conexion$, $c1$, $c2$)}{dameUno((compusQueConecta(conexion)) - \{$c1$\})}
 
 \tadAxioma{estanConectadas($conexion$, $c1$, $c2$)}{conectaA($conexion$, $c1$) $\wedge$ conectaA($conexion$, $c2$) $\wedge$ ($c1$ $\neq$ $c2$)}

\end{tad}
